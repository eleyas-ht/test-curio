---
// Sort control — native select inside a GET form (resets cursor on
// change). Extra params (e.g. search q) are preserved via hidden inputs.
import type { SortOption } from '~/lib/shopify/types';
import Icon from '~/components/ui/Icon.astro';

interface Props {
  options: SortOption[];
  value: string;
  action: string;
  hidden?: Record<string, string>;
}
const { options, value, action, hidden = {} } = Astro.props;
---

<form method="get" action={action} class="relative inline-flex items-center">
  {Object.entries(hidden).map(([k, v]) => <input type="hidden" name={k} value={v} />)}
  <label for="sort" class="eyebrow sr-only">Sort</label>
  <div
    class="relative inline-flex items-center gap-2 rounded-md border border-sand-deep bg-warm-white pl-3 pr-2 transition-fluid hover:border-clay"
  >
    <span class="eyebrow text-muted-light">Sort</span>
    <select
      id="sort"
      name="sort"
      onchange="this.form.submit()"
      class="h-10 cursor-pointer appearance-none bg-transparent pr-5 text-sm text-ink outline-none"
    >
      {
        options.map((o) => (
          <option value={o.value} selected={o.value === value}>
            {o.label}
          </option>
        ))
      }
    </select>
    <Icon name="chevronDown" size={15} class="pointer-events-none -ml-5 text-muted" />
  </div>
</form>
